<!-- этот блок или здесь или в home.handlebars-->
<!--
<h1 class="title">Categories</h1>

<div class="categories">  
    {{#if categories.length}}
      {{#each categories}}
     
      <div class="categories__item" style="margin: 25px;">
       <a href="{{link}}">
        <div class="categories__image-box" style="width: 50px; height: 50px;">
          <img class="categories__image" style="max-width: 100%; height: 100%;" src="{{image}}" alt="Product Image">
        </div>
        <div class="categories__name"><span class="categories__name-span">{{name}}</span></div>
        </a>
      </div>
     
      {{/each}}
    {{else}}
      <p>No categories found</p>
    {{/if}}
</div>  
-->

